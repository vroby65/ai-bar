# Project instructions

Before changing this repository, read `agent/README.md` completely. Keep that guide
in sync when a change alters the architecture, configuration schema, development
workflow, runtime invariants, or verification commands.
